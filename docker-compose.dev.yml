services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: psh
      POSTGRES_PASSWORD: devonly
      POSTGRES_DB: psh_petty_cash
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
